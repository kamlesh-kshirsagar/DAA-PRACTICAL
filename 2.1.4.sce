/*n=int(input("enter the number"));

select(n)
    case "1" if(n<0) then
    disp("the entered number is negative number");
    break;
    case "2" if(n>0) then
        disp("entwred number is positive");
        break;
    case "3" if(n==0) then
            disp("entered number is zero");
            break;
            end*/
num=input("enter the numvber");
select num
case 1
    disp("positive");
    break;
case 2
    disp("negative");
    break;
end
